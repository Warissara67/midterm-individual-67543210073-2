# Library Management System - Layered Architecture

## 📋 Project Information
- **Student Name:** [ชื่อ-นามสกุล]
- **Student ID:** [รหัสนักศึกษา]
- **Course:** ENGSE207 Software Architecture

## 🏗️ Architecture Style
Layered Architecture (3-tier)
ระบบถูกออกแบบโดยแยกความรับผิดชอบของโค้ดออกเป็น 3 ชั้น ได้แก่ Presentation, Business Logic และ Data Access เพื่อให้โค้ดเป็นระเบียบและดูแลรักษาง่าย

## 📂 Project Structure
โครงสร้างโปรเจกต์แบ่งออกเป็น 3 Layer หลักดังนี้

### **Presentation Layer**
จัดการ HTTP Request/Response, Routing และ Error Handling
(routes, controllers, middlewares)

### **Business Logic Layer**
จัดการกฎทางธุรกิจ การตรวจสอบข้อมูล และกระบวนการทำงานของระบบ
(services, validators)

### **Data Access Layer**
จัดการการเชื่อมต่อฐานข้อมูลและคำสั่ง SQL
(repositories, database)

## 🎯 Refactoring Summary

### ปัญหาของ Monolithic (เดิม):
- โค้ดทั้งหมดอยู่ในไฟล์เดียว ทำให้ไฟล์มีขนาดใหญ่และซับซ้อน

- Business Logic, Database และ HTTP Handling ปนกัน

- แก้ไขโค้ดส่วนหนึ่งอาจกระทบส่วนอื่น

- ยากต่อการบำรุงรักษาและขยายระบบ

- การทำงานร่วมกันเป็นทีมทำได้ยาก

### วิธีแก้ไขด้วย Layered Architecture:
- แยกโค้ดออกเป็น Presentation, Business Logic และ Data Access

- Controller ทำหน้าที่รับ–ส่ง HTTP เท่านั้น

- Service จัดการ Business Logic และกฎของระบบ

- Repository จัดการการเข้าถึงฐานข้อมูลโดยเฉพาะ

- ใช้ Middleware กลางสำหรับจัดการ Error
- 
### ประโยชน์ที่ได้รับ:
- โค้ดเป็นระเบียบ อ่านง่าย และดูแลรักษาง่าย

- ลดผลกระทบเมื่อมีการแก้ไขโค้ด

- รองรับการทำงานเป็นทีมได้ดี

- ง่ายต่อการขยายระบบในอนาคต

- สอดคล้องกับหลัก Separation of Concerns

## 🚀 How to Run

```
# 1. Clone repository
git clone https://github.com/Warissara67/midterm-individual-67543210073-2.git

# 2. Install dependencies
npm install

# 3. Run server
npm start

# 4. Test API
# Open browser: http://localhost:3000
```

## 📝 API Endpoints
GET	`/api/books`	ดึงข้อมูลหนังสือทั้งหมด
GET	`/api/books/:id`	ดึงข้อมูลหนังสือตาม ID
POST	`/api/books`	เพิ่มหนังสือใหม่
PUT	`/api/books/:id`	แก้ไขข้อมูลหนังสือ
PATCH	`/api/books/:id/borrow`	ยืมหนังสือ
PATCH	`/api/books/:id/return`	คืนหนังสือ
DELETE	`/api/books/:id`	ลบหนังสือ
